# hadolint configuration
# https://github.com/hadolint/hadolint#configure
ignored:
  # DL3006: Always tag the version of an image explicitly (we use ARG-based FROM)
  - DL3006
  # SC2035: Use ./*glob* so names with dashes won't become options
  # Intentional: *.jar in WORKDIR /tmp/target is safe, only one JAR exists
  - SC2035
